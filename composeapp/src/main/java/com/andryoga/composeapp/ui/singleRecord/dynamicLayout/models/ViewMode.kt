package com.andryoga.composeapp.ui.singleRecord.dynamicLayout.models

enum class ViewMode {
    // when user is editing an existing record
    EDIT,

    // when user is viewing a saved record
    VIEW,

    // when user is creating a new record
    NEW
}